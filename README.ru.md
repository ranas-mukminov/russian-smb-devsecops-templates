# 🛡️ DevSecOps шаблоны для малого бизнеса в РФ

[![CI](https://github.com/ranas-mukminov/russian-smb-devsecops-templates/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/russian-smb-devsecops-templates/actions/workflows/ci.yml)
[![Security](https://github.com/ranas-mukminov/russian-smb-devsecops-templates/actions/workflows/security.yml/badge.svg)](https://github.com/ranas-mukminov/russian-smb-devsecops-templates/actions/workflows/security.yml)
[![License](https://img.shields.io/badge/license-Apache%202.0-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/python-3.10%2B-blue.svg)](https://www.python.org/downloads/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

## Описание

Готовые к продакшену DevSecOps-шаблоны, специально разработанные для команд малого и среднего бизнеса в России. Репозиторий предлагает две основные подсистемы: комплексный стек логирования, адаптированный под распространённое российское ПО (nginx, 1С, 1C-Bitrix, почтовые серверы, VPN), и шаблоны безопасных CI/CD-пайплайнов со встроенным сканированием безопасности. Обе подсистемы не привязаны к конкретным поставщикам, открыты и не требуют проприетарных лицензий или облачных вендоров.

Проект решает реальные задачи DevOps-команд российского малого бизнеса: централизованное логирование для разнородных стеков, аудит-траектории для соответствия требованиям, автоматизированное тестирование безопасности без дорогих коммерческих инструментов.

## Основные возможности

- **Стек логирования «из коробки» на Loki или ELK**: Docker Compose конфигурации для Loki+Promtail+Grafana или Elasticsearch+Logstash+Kibana+Filebeat
- **Парсеры для российского бизнес-ПО**: Преднастроенные пайплайны обработки логов для nginx, 1С, 1C-Bitrix, Postfix/Dovecot, OpenVPN
- **Готовые дашборды Grafana**: Визуализации с русскими подписями для веб-трафика, ошибок, провалов авторизации, VPN-сессий, производительности 1С
- **Безопасные CI/CD шаблоны**: пайплайны GitHub Actions и GitLab CI с юнит-тестами, линтерами, SAST (Bandit, Semgrep), проверкой зависимостей (pip-audit, npm audit), сканированием контейнеров (Trivy)
- **Поддержка нескольких языков**: Шаблоны для PHP/1C-Bitrix, Python и Node.js проектов
- **Вендоронезависимая архитектура**: Без привязки к облачным платформам; работает on-premises или у любого облачного провайдера
- **AI-помощники для конфигураций**: Опциональные интерфейсы для генерации кастомных парсеров и пайплайнов с помощью внешних AI API (требуется свой API-ключ)
- **Двуязычная документация**: Все конфигурации и дашборды содержат русские комментарии и названия

## Архитектура и компоненты

Репозиторий состоит из двух независимых подсистем, которые можно разворачивать отдельно или вместе:

### Стек логирования (`logging_stack/`)

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Источники      │────▶│  Loki/ELK Стек  │────▶│  Grafana/Kibana │
│  логов (nginx,  │     │  (Promtail/     │     │   (Дашборды)    │
│  1С, Bitrix,    │     │   Filebeat)     │     │                 │
│  VPN)           │     │                 │     │                 │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

- **Вариант Loki**: Grafana Loki 2.9.4, Promtail 2.9.4, Grafana 10.1.5
- **Вариант ELK**: Elasticsearch 8.x, Logstash 8.x, Kibana 8.x, Filebeat 8.x
- **Парсеры**: Grok/LogQL пайплайны для access-логов, логов ошибок, логов приложений
- **Дашборды**: Готовые визуализации для анализа трафика, отслеживания ошибок, событий безопасности

### CI шаблоны безопасности (`ci_security_templates/`)

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Исходный код   │────▶│  CI Пайплайн    │────▶│  Безопасная     │
│  (PHP/Py/Node)  │     │ (Тесты, SAST,   │     │  сборка +       │
│                 │     │  Зависимости)   │     │  Реестр         │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

- **GitHub Actions workflows**: Composite actions и reusable workflows для PHP/Bitrix, Python, Node.js
- **GitLab CI шаблоны**: Примеры `.gitlab-ci.yml` и shared includes для многоэтапных пайплайнов
- **Этапы безопасности**: Статический анализ (Bandit, Semgrep), аудит зависимостей (pip-audit, npm audit), сканирование контейнеров (Trivy)
- **Проверки качества**: Линтинг кода (Ruff, ESLint, PHPStan), юнит-тесты (PHPUnit, pytest, Jest)

Обе подсистемы используют Docker Compose для локального/on-premises развёртывания и не требуют внешних SaaS-зависимостей.

## Требования

### Операционная система
- **Linux дистрибутивы**: Ubuntu 20.04/22.04 LTS, Debian 11/12, RHEL 8/9, Rocky Linux 8/9, Alma Linux 8/9
- **Ядро**: 4.15+ (для поддержки контейнеров)
- **Архитектура**: x86_64 (amd64)

### Системные ресурсы
- **CPU**: Минимум 2 ядра (4+ рекомендуется для ELK стека)
- **RAM**: 
  - Loki стек: минимум 4 ГБ, рекомендуется 8 ГБ
  - ELK стек: минимум 8 ГБ, рекомендуется 16 ГБ
- **Диск**: Минимум 50 ГБ свободного пространства для хранения логов (настройте согласно политике хранения)

### Программные зависимости
- **Docker**: 20.10+ 
- **Docker Compose**: 2.0+ (идёт с Docker Desktop или устанавливается отдельно)
- **Python**: 3.10+ (для валидации и вспомогательных инструментов)
- **Git**: 2.30+ (для клонирования репозитория)

### Сеть и доступ
- **Порты**: 3000 (Grafana), 3100 (Loki), 5601 (Kibana), 9200 (Elasticsearch)
- **Права доступа**: sudo/root для установки Docker и монтирования каталогов логов
- **Доступ в Интернет**: Требуется для загрузки Docker-образов из Docker Hub и Grafana registry

### Опционально для CI/CD шаблонов
- **GitHub аккаунт** или **GitLab инстанс** (self-hosted или GitLab.com)
- **Container registry**: Docker Hub, GitHub Container Registry, GitLab Registry или private registry

## Быстрый старт (TL;DR)

### Стек логирования (Loki + Grafana)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/ranas-mukminov/russian-smb-devsecops-templates.git
cd russian-smb-devsecops-templates

# 2. Перейти в Loki стек
cd logging_stack/loki

# 3. Создать каталоги логов (или скорректировать пути в docker-compose.loki.yml)
sudo mkdir -p /var/log/{nginx,bitrix,onec,mail,vpn}
sudo chmod 755 /var/log/{nginx,bitrix,onec,mail,vpn}

# 4. Запустить стек
docker compose -f docker-compose.loki.yml up -d

# 5. Открыть Grafana
# Откройте http://localhost:3000 в браузере
# Учётные данные по умолчанию: admin / admin (смените немедленно!)
```

### CI/CD пайплайн (GitHub Actions)

```bash
# 1. Скопировать нужный шаблон workflow в ваш проект
cp ci_security_templates/github/python/ci.yml <ВАШ_ПРОЕКТ>/.github/workflows/

# 2. Настроить GitHub Secrets в настройках репозитория:
# - PYPI_TOKEN (если публикуете пакеты)
# - DOCKER_USERNAME, DOCKER_PASSWORD (для сборки контейнеров)
# - Любые кастомные секреты для вашей инфраструктуры

# 3. Отправить в GitHub - workflow запустится автоматически при push/PR
git add .github/workflows/ci.yml
git commit -m "Добавить безопасный CI пайплайн"
git push
```

### CI/CD пайплайн (GitLab CI)

```bash
# 1. Скопировать нужный шаблон в корень проекта
cp ci_security_templates/gitlab/python/.gitlab-ci.yml <ВАШ_ПРОЕКТ>/

# 2. Настроить GitLab CI/CD Variables в настройках проекта:
# - CI_REGISTRY_USER, CI_REGISTRY_PASSWORD (для сборки контейнеров)
# - Любые кастомные переменные для вашей инфраструктуры

# 3. Отправить в GitLab - пайплайн запустится автоматически
git add .gitlab-ci.yml
git commit -m "Добавить безопасный CI пайплайн"
git push
```

## Подробная установка

### Установка стека логирования на Ubuntu / Debian

#### Предварительные требования

```bash
# Обновить индекс пакетов
sudo apt update

# Установить Docker
sudo apt install -y docker.io docker-compose-plugin

# Запустить и включить Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавить вашего пользователя в группу docker (требуется logout/login)
sudo usermod -aG docker $USER
```

#### Развернуть Loki стек

```bash
# Клонировать репозиторий
git clone https://github.com/ranas-mukminov/russian-smb-devsecops-templates.git
cd russian-smb-devsecops-templates/logging_stack/loki

# Просмотреть и настроить docker-compose.loki.yml
# - Откорректировать пути к источникам логов (/var/log/nginx, /var/log/bitrix и т.д.)
# - Изменить пароль администратора Grafana по умолчанию в секции environment
# - Настроить монтирование volumes под вашу инфраструктуру

# Создать необходимые каталоги логов
sudo mkdir -p /var/log/{nginx,bitrix,onec,mail,vpn}
sudo chmod -R 755 /var/log/{nginx,bitrix,onec,mail,vpn}

# Запустить стек
docker compose -f docker-compose.loki.yml up -d

# Проверить, что все контейнеры запущены
docker compose -f docker-compose.loki.yml ps

# Проверить логи на наличие ошибок
docker compose -f docker-compose.loki.yml logs
```

#### Развернуть ELK стек (альтернатива)

```bash
cd logging_stack/elk

# Увеличить vm.max_map_count для Elasticsearch (обязательно)
sudo sysctl -w vm.max_map_count=262144
echo "vm.max_map_count=262144" | sudo tee -a /etc/sysctl.conf

# Просмотреть и настроить docker-compose.elk.yml
# Запустить стек
docker compose -f docker-compose.elk.yml up -d

# Дождаться инициализации Elasticsearch (проверить через docker logs)
docker compose -f docker-compose.elk.yml logs elasticsearch

# Открыть Kibana по адресу http://localhost:5601
```

### Установка на RHEL / Rocky / Alma Linux

```bash
# Установить Docker
sudo dnf config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo
sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Запустить и включить Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker
sudo usermod -aG docker $USER

# Продолжить развёртывание Loki или ELK как выше
```

### Установка CI/CD шаблонов

CI/CD шаблоны не требуют установки — их копируют напрямую в репозиторий проекта. См. раздел «Настройка» для деталей кастомизации шаблонов.

## Настройка

### Настройка стека логирования

#### Loki + Promtail

Отредактируйте `logging_stack/loki/promtail/promtail-config.yml` для настройки источников логов:

```yaml
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Логи доступа nginx
  - job_name: nginx-access
    static_configs:
      - targets:
          - localhost
        labels:
          job: nginx
          __path__: /var/log/nginx/access.log
    pipeline_stages:
      - match:
          selector: '{job="nginx"}'
          stages:
            - regex:
                expression: '^(?P<remote_addr>[\w\.]+) - (?P<remote_user>.*) \[(?P<time_local>.*)\] "(?P<method>\w+) (?P<request>.*) (?P<protocol>.*)" (?P<status>\d+) (?P<body_bytes_sent>\d+) "(?P<http_referer>.*)" "(?P<http_user_agent>.*)"'
            - labels:
                method:
                status:
```

Настройте `__path__` в соответствии с реальными расположениями ваших файлов логов.

#### Источники данных Grafana

Loki автоматически настраивается как источник данных. Для добавления кастомных источников данных отредактируйте `logging_stack/grafana/provisioning/datasources/loki.yml`.

#### Переменные окружения

Основная конфигурация через `docker-compose.loki.yml`:

```yaml
# Учётные данные администратора Grafana (СМЕНИТЬ НЕМЕДЛЕННО)
GF_SECURITY_ADMIN_USER: admin
GF_SECURITY_ADMIN_PASSWORD: <ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ>

# Опционально: SMTP для алертинга
# GF_SMTP_ENABLED: true
# GF_SMTP_HOST: <ВАШ_SMTP_СЕРВЕР>:587
# GF_SMTP_USER: <ВАШ_SMTP_ПОЛЬЗОВАТЕЛЬ>
# GF_SMTP_PASSWORD: <ВАШ_SMTP_ПАРОЛЬ>
```

### Настройка CI/CD шаблонов

#### GitHub Actions (пример Python)

Отредактируйте `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install ruff bandit
      - name: Запуск Ruff
        run: ruff check .
      - name: Запуск Bandit (SAST)
        run: bandit -r src/

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Установка зависимостей
        run: |
          pip install -e .
          pip install pytest pytest-cov
      - name: Запуск тестов
        run: pytest --cov=src tests/

  security:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Аудит зависимостей
        run: |
          pip install pip-audit
          pip-audit
```

Необходимые секреты (настроить в настройках GitHub-репозитория):
- `PYPI_TOKEN` - для публикации пакетов
- `DOCKER_USERNAME`, `DOCKER_PASSWORD` - для аутентификации в container registry

#### GitLab CI (пример Python)

Отредактируйте `.gitlab-ci.yml`:

```yaml
stages:
  - lint
  - test
  - security
  - build

variables:
  PYTHON_VERSION: "3.11"

lint:
  stage: lint
  image: python:${PYTHON_VERSION}
  script:
    - pip install ruff
    - ruff check .

test:
  stage: test
  image: python:${PYTHON_VERSION}
  script:
    - pip install -e .
    - pip install pytest pytest-cov
    - pytest --cov=src tests/

security:sast:
  stage: security
  image: python:${PYTHON_VERSION}
  script:
    - pip install bandit
    - bandit -r src/

security:dependencies:
  stage: security
  image: python:${PYTHON_VERSION}
  script:
    - pip install pip-audit
    - pip-audit

security:container:
  stage: security
  image: aquasec/trivy:latest
  script:
    - trivy image <НАЗВАНИЕ_ВАШЕГО_ОБРАЗА>
```

Необходимые GitLab CI/CD переменные:
- `CI_REGISTRY_USER`, `CI_REGISTRY_PASSWORD` - для GitLab Container Registry
- Кастомные переменные для вашей инфраструктуры

## Использование и типовые задачи

### Доступ к веб-интерфейсам

#### Grafana (Loki стек)
- **URL**: `http://<IP_ВАШЕГО_СЕРВЕРА>:3000`
- **Учётные данные по умолчанию**: `admin` / `admin` (смените при первом входе)
- **Дашборды**: Навигация → "Dashboards" → "Browse" → Выбрать преимпортированные дашборды

#### Kibana (ELK стек)
- **URL**: `http://<IP_ВАШЕГО_СЕРВЕРА>:5601`
- **Аутентификация по умолчанию отсутствует** (настройте X-Pack security для продакшена)
- **Паттерны индексов**: Автоматически созданы для индексов `logstash-*`

### Управление стеком логирования

#### Запуск/Остановка сервисов

```bash
# Запустить Loki стек
cd logging_stack/loki
docker compose -f docker-compose.loki.yml up -d

# Остановить Loki стек
docker compose -f docker-compose.loki.yml down

# Перезапустить конкретный сервис
docker compose -f docker-compose.loki.yml restart grafana

# Просмотреть логи сервиса
docker compose -f docker-compose.loki.yml logs -f promtail
```

#### Импорт кастомных дашбордов Grafana

```bash
# Разместить JSON дашборда в logging_stack/grafana/dashboards/
cp мой-кастомный-дашборд.json logging_stack/grafana/dashboards/

# Перезапустить Grafana для загрузки дашборда
docker compose -f docker-compose.loki.yml restart grafana

# Или импортировать через UI Grafana: Dashboards → Import → Upload JSON file
```

#### Запросы логов с помощью LogQL (Loki)

Доступ: Grafana → Explore, затем использовать LogQL запросы:

```logql
# Все ошибки nginx 5xx за последний час
{job="nginx"} |= "status=5" | logfmt | status >= 500

# Неудачные попытки SSH-входа
{job="auth"} |= "Failed password"

# Ошибки 1С
{job="onec"} |~ "error|exception" | line_format "{{.timestamp}} {{.message}}"
```

### Использование CI/CD шаблонов

#### Локальный запуск workflows (GitHub Actions)

```bash
# Установить act (локальный runner GitHub Actions)
# https://github.com/nektos/act
curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash

# Запустить workflow локально
cd <ВАШ_ПРОЕКТ>
act -j lint  # Запустить конкретную job
```

#### Тестирование GitLab CI локально

```bash
# Установить gitlab-runner
# https://docs.gitlab.com/runner/install/
curl -L https://packages.gitlab.com/install/repositories/runner/gitlab-runner/script.deb.sh | sudo bash
sudo apt install gitlab-runner

# Валидировать синтаксис .gitlab-ci.yml
gitlab-runner exec shell lint
```

#### Кастомизация сканеров безопасности

Добавить кастомную конфигурацию Bandit (`.bandit`):

```ini
[bandit]
exclude_dirs = /test,/tests,/venv
skips = B404,B603
```

Добавить файл игнорирования Trivy (`.trivyignore`):

```text
# Игнорировать конкретные CVE
CVE-2023-12345

# Игнорировать низкую серьёзность
LOW
```

## Обновление

### Обновление стека логирования

```bash
cd logging_stack/loki

# Загрузить последние образы
docker compose -f docker-compose.loki.yml pull

# Пересоздать контейнеры с новыми образами
docker compose -f docker-compose.loki.yml up -d

# Проверить, что все сервисы работают
docker compose -f docker-compose.loki.yml ps
```

### Обновление CI/CD шаблонов

```bash
# Загрузить последний репозиторий шаблонов
cd russian-smb-devsecops-templates
git pull origin main

# Просмотреть изменения
git log --oneline

# Скопировать обновлённые шаблоны в ваш проект
# ПРОВЕРЬТЕ ИЗМЕНЕНИЯ ПЕРЕД ПЕРЕЗАПИСЬЮ
diff ci_security_templates/github/python/ci.yml <ВАШ_ПРОЕКТ>/.github/workflows/ci.yml

# Вручную объединить изменения или заменить шаблон
cp ci_security_templates/github/python/ci.yml <ВАШ_ПРОЕКТ>/.github/workflows/
```

### Критические изменения

Проверяйте `CHANGELOG.md` на критические изменения между версиями. Крупные обновления могут требовать:
- Миграции конфигурационных файлов
- Обновления схемы БД (для ELK стека)
- Регенерации секретных ключей
- Переимпорта дашбордов

## Логи, мониторинг, устранение неполадок

### Просмотр логов

#### Docker логи

```bash
# Просмотреть логи всех контейнеров
docker compose -f docker-compose.loki.yml logs

# Следить за логами конкретного сервиса
docker compose -f docker-compose.loki.yml logs -f promtail

# Просмотреть последние 100 строк
docker compose -f docker-compose.loki.yml logs --tail=100 loki
```

#### Логи systemd (если Docker запущен через systemd)

```bash
# Логи демона Docker
sudo journalctl -u docker -f

# Проверить статус сервиса Docker
sudo systemctl status docker
```

### Типичные проблемы и решения

#### Grafana: "Data source not found"

**Проблема**: Источник данных Loki не настроен автоматически.

**Решение**:
```bash
# Проверить, что Loki запущен и доступен
curl http://localhost:3100/ready

# Добавить источник данных вручную: UI Grafana → Configuration → Data Sources → Add Loki
# URL: http://loki:3100
```

#### Promtail: "permission denied" при чтении файлов логов

**Проблема**: Контейнер Promtail не может получить доступ к файлам логов хоста.

**Решение**:
```bash
# Исправить права на файлы логов
sudo chmod 644 /var/log/nginx/*.log

# Или добавить пользователя Promtail в группу логов
sudo usermod -aG adm <ПОЛЬЗОВАТЕЛЬ_PROMTAIL>

# Настроить контекст SELinux (RHEL/CentOS)
sudo chcon -R -t svirt_sandbox_file_t /var/log/nginx/
```

#### Порт 3000 уже используется

**Проблема**: Другой сервис использует порт Grafana.

**Решение**:
```bash
# Определить процесс, использующий порт 3000
sudo lsof -i :3000

# Убить конфликтующий процесс или изменить порт Grafana в docker-compose.loki.yml:
# ports:
#   - "3001:3000"  # Сопоставить с портом хоста 3001
```

#### Elasticsearch не запускается: "max virtual memory areas too low"

**Проблема**: Слишком низкое значение vm.max_map_count в ядре Linux.

**Решение**:
```bash
sudo sysctl -w vm.max_map_count=262144
echo "vm.max_map_count=262144" | sudo tee -a /etc/sysctl.conf
```

#### CI пайплайн падает с "pip-audit not found"

**Проблема**: pip-audit не установлен в CI окружении.

**Решение**: Убедитесь, что шаг установки существует в workflow:
```yaml
- name: Установить pip-audit
  run: pip install pip-audit
```

#### Нет данных в дашбордах Grafana

**Проблема**: Логи не поступают.

**Решение**:
```bash
# Проверить, что targets Promtail активны
curl http://localhost:9080/targets  # endpoints метрик Promtail

# Проверить существование и доступность файлов логов
ls -la /var/log/nginx/

# Проверить логи Promtail на наличие ошибок
docker compose -f docker-compose.loki.yml logs promtail | grep -i error
```

## Замечания по безопасности

### Обязательный чеклист безопасности

- [ ] **Смените пароли по умолчанию**: Пароль администратора Grafana должен быть изменён немедленно после первого входа
- [ ] **Ограничьте сетевой доступ**: Используйте правила файрвола (ufw, firewalld) для ограничения доступа к портам 3000, 3100, 5601, 9200 только доверенным IP
- [ ] **Включите HTTPS**: Используйте reverse proxy (nginx, Traefik) с SSL-сертификатами Let's Encrypt для веб-интерфейсов
- [ ] **Защитите Docker socket**: Не предоставляйте Docker socket (/var/run/docker.sock) контейнерам без крайней необходимости
- [ ] **Управление секретами**: Никогда не коммитьте пароли, API-ключи или токены в Git; используйте переменные окружения или секреты CI/CD
- [ ] **Контроль доступа к логам**: Убедитесь, что файлы логов на хосте не содержат чувствительных данных (банковские карты, пароли); используйте парсеры для редактирования при необходимости
- [ ] **Регулярные обновления**: Обновляйте Docker-образы; подписывайтесь на security advisories для Grafana, Loki, Elasticsearch
- [ ] **Аудит логов**: Включите логирование аудита в Grafana и регулярно проверяйте паттерны доступа
- [ ] **VPN или приватная сеть**: Для продакшена запускайте стек логирования во внутренней сети или за VPN, не выставляя в публичный интернет

### Рекомендации для продакшена

- Используйте сильные, уникальные пароли для всех сервисов (минимум 16 символов, буквенно-цифровые + символы)
- Настройте аутентификацию Grafana с LDAP или OAuth (GitHub, GitLab, Google) вместо локальных пользователей
- Включите ограничение анонимного доступа Grafana и ролевое управление доступом (RBAC)
- Для ELK стека настройте X-Pack security (базовая аутентификация, TLS, логи аудита) - см. документацию Elasticsearch
- Регулярно ротируйте API-токены и credentials, используемые в CI/CD пайплайнах
- Установите политики хранения логов в соответствии с местным законодательством о защите данных (GDPR, 152-ФЗ РФ)
- Делайте резервные копии дашбордов Grafana и данных Loki/Elasticsearch на отдельное хранилище
- Мониторьте использование ресурсов и настройте алерты о нехватке места на диске для предотвращения переполнения логами

## Структура проекта

```
russian-smb-devsecops-templates/
├── ci_security_templates/          # CI/CD шаблоны безопасности
│   ├── github/                     # GitHub Actions workflows
│   │   ├── php_bitrix/             # Проекты PHP на 1C-Bitrix
│   │   ├── python/                 # Python проекты
│   │   └── node/                   # Node.js проекты
│   ├── gitlab/                     # GitLab CI шаблоны
│   │   ├── php_bitrix/
│   │   ├── python/
│   │   └── node/
│   └── ai_pipeline_helpers/        # Инструменты AI-генерации пайплайнов
├── logging_stack/                  # Инфраструктура централизованного логирования
│   ├── loki/                       # Loki + Promtail + Grafana
│   │   ├── docker-compose.loki.yml
│   │   ├── promtail/               # Конфигурация и пайплайны Promtail
│   │   └── grafana/                # Дашборды и источники данных
│   ├── elk/                        # Elasticsearch + Logstash + Kibana
│   │   ├── docker-compose.elk.yml
│   │   ├── logstash/               # Logstash пайплайны
│   │   └── kibana/                 # Kibana дашборды
│   ├── parsers/                    # Конфигурации парсеров логов (nginx, 1С, Bitrix, VPN)
│   └── ai_helpers/                 # AI-генерация парсеров
├── tools/                          # Утилиты валидации и генерации
│   ├── validate_ci_templates.py    # Валидация синтаксиса CI шаблонов
│   ├── validate_logging_configs.py # Валидация конфигураций логирования
│   └── generate_example_project.py # Создание каркаса примера проекта
├── scripts/                        # Вспомогательные скрипты
│   ├── lint.sh                     # Запуск линтеров на репозитории
│   ├── security_scan.sh            # Запуск Bandit security scan
│   └── perf_check.sh               # Валидация производительности
├── tests/                          # Юнит и интеграционные тесты
│   ├── unit/                       # Юнит-тесты для инструментов
│   └── integration/                # Интеграционные тесты для шаблонов
├── README.md                       # Английская версия
├── README.ru.md                    # Этот файл (русская версия)
├── LICENSE                         # Apache 2.0 лицензия
├── CONTRIBUTING.md                 # Правила внесения изменений
├── LEGAL.md                        # Юридическая оговорка и условия использования
└── CHANGELOG.md                    # История версий
```

## Roadmap / Планы

- [ ] Добавить поддержку Vector.dev как альтернативного сборщика логов
- [ ] Шаблоны сбора логов Windows Server (Event Log, IIS)
- [ ] Kubernetes/k3s манифесты развёртывания (Helm charts)
- [ ] Преднастроенные правила алертинга для типичных инцидентов (нехватка места на диске, провалы авторизации, остановка сервисов)
- [ ] Примеры интеграции с мониторингом Zabbix и Prometheus
- [ ] Terraform модули для автоматизированного развёртывания инфраструктуры
- [ ] Поддержка дополнительного российского бизнес-ПО (например, Terraform, LDAP/FreeIPA)
- [ ] Примеры GitOps workflow с ArgoCD/Flux

Приветствуются вклады в реализацию этих пунктов roadmap! См. `CONTRIBUTING.md`.

## Как внести вклад

Мы приветствуем вклады от сообщества! Вот как вы можете помочь:

### Сообщение о проблемах

- Используйте GitHub Issues для сообщений об ошибках или запросов функций
- Предоставляйте подробную информацию: версия ОС, версия Docker, логи ошибок, шаги воспроизведения
- Проверяйте существующие issues во избежание дубликатов

### Отправка Pull Request

1. Сделайте fork репозитория и создайте feature branch:
   ```bash
   git checkout -b feature/моя-новая-функция
   ```

2. Внесите изменения, следуя правилам оформления кода:
   - **Python**: Используйте type hints, следуйте PEP 8, используйте `ruff` для линтинга
   - **YAML/JSON**: Отступы 2 пробела, согласованное именование ключей
   - **Shell скрипты**: POSIX-совместимый bash, используйте ShellCheck

3. Протестируйте ваши изменения:
   ```bash
   # Запустить линтер
   ./scripts/lint.sh

   # Запустить сканер безопасности
   ./scripts/security_scan.sh

   # Запустить тесты
   pytest
   ```

4. Обновите документацию (README.md, README.ru.md) и CHANGELOG.md

5. Откройте pull request с чётким описанием изменений и обоснованием

### Стиль кода

- Конфигурации должны быть свободны от секретов (используйте плейсхолдеры вида `<ВАШ_ТОКЕН>`)
- Включайте русские комментарии для конфигов российского ПО (1С, Bitrix)
- Добавляйте английские комментарии для широкой применимости
- Пишите юнит-тесты для новых Python-инструментов и валидаторов

## Лицензия

Этот проект лицензирован по **Apache License 2.0**. См. файл [LICENSE](LICENSE) для подробностей.

Ключевые моменты:
- Свободно использовать, модифицировать и распространять
- Необходимо сохранять уведомления об авторских правах и лицензии
- Предоставляется «как есть» без гарантий
- Участники предоставляют патентную лицензию на свои вклады

## Автор и коммерческая поддержка

**Автор**: [Ранас Мукминов](https://github.com/ranas-mukminov)  
**Сайт**: [run-as-daemon.ru](https://run-as-daemon.ru)

### Коммерческие услуги

Для продакшн-развёртывания, кастомизации и постоянной поддержки доступны профессиональные DevOps/SRE услуги:

- **Развёртывание инфраструктуры логирования**: Полная настройка централизованного логирования для nginx, 1С, 1C-Bitrix, почтовых серверов, VPN с кастомными парсерами и дашбордами
- **Внедрение безопасного CI/CD**: End-to-end настройка пайплайнов с тестированием, SAST, сканированием зависимостей и контейнеров, автоматизацией развёртывания
- **Аудит инфраструктуры**: Проверка безопасности и производительности существующей DevOps-инфраструктуры
- **Обучение команды**: Воркшопы по best practices DevSecOps, анализу логов, безопасности CI/CD
- **Кастомная разработка**: Разработка парсеров, дашбордов, правил алертинга и интеграций под ваш специфический стек

Обращайтесь через [run-as-daemon.ru](https://run-as-daemon.ru) или GitHub-профиль для запросов.

---

**Отказ от ответственности**: Этот проект не имеет официальной связи с компаниями 1С, 1C-Bitrix, GitHub, GitLab, Grafana Labs, Elastic NV и другими упомянутыми компаниями. Все торговые марки принадлежат их владельцам.
